[build]
  base = "astro"
  command = "cd .. && rm -rf moltbook_data && git clone --depth 1 https://github.com/ExtraE113/moltbook_data.git moltbook_data && cd astro && npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"